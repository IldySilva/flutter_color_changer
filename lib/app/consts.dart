/// The maximum opacity value for colors (fully opaque).
const double fullOpacityValue = 1.0;

/// The threshold value for determining high luminance.
/// A luminance value greater than this indicates a light color.
const double highLuminanceThreshold = 0.5;

/// The default duration for animations in the application.
const Duration defaultAnimationDuration = Duration(milliseconds: 400);
